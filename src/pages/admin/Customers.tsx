import {useEffect,useState} from 'react';
type Customer={id:number;name:string;email:string;booking_count:number;total_spent:number};
export default function AdminCustomers(){const [rows,setRows]=useState<Customer[]>([]);useEffect(()=>{fetch('/api/admin/customers').then(r=>r.json()).then(setRows)},[]);return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-white">ลูกค้า</h1><p className="text-zinc-500 text-sm mt-1">ข้อมูลลูกค้าจากฐานข้อมูลจริง</p></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4">{rows.map(c=><div key={c.id} className="bg-[#111114] border border-white/6 rounded-xl p-5"><h3 className="text-white font-semibold">{c.name}</h3><p className="text-zinc-500 text-sm">{c.email}</p><div className="mt-4 flex justify-between text-sm"><span className="text-zinc-400">การจอง {c.booking_count} ครั้ง</span><span className="text-violet-400">฿{c.total_spent}</span></div></div>)}</div></div>}
