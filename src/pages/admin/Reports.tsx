import {useEffect,useState} from 'react';
export default function AdminReports(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/reports').then(r=>r.json()).then(setRows)},[]);const revenue=rows.reduce((a,r)=>a+Number(r.revenue),0),bookings=rows.reduce((a,r)=>a+Number(r.bookings),0);return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-white">รายงาน</h1><p className="text-zinc-500 text-sm mt-1">สรุปข้อมูลจากฐานข้อมูลจริง</p></div><div className="grid md:grid-cols-2 gap-4"><div className="bg-[#111114] border border-white/6 rounded-xl p-5"><p className="text-zinc-500">การจองในข้อมูลที่แสดง</p><p className="text-3xl font-bold text-white mt-2">{bookings}</p></div><div className="bg-[#111114] border border-white/6 rounded-xl p-5"><p className="text-zinc-500">รายได้รวม</p><p className="text-3xl font-bold text-emerald-400 mt-2">฿{revenue}</p></div></div><div className="bg-[#111114] border border-white/6 rounded-xl overflow-auto"><table className="w-full text-sm"><thead><tr className="text-zinc-500 border-b border-white/6"><th className="p-4 text-left">วันที่</th><th className="p-4 text-left">จำนวนจอง</th><th className="p-4 text-left">รายได้</th></tr></thead><tbody>{rows.map(r=><tr key={r.date} className="border-b border-white/4"><td className="p-4 text-zinc-300">{r.date}</td><td className="p-4 text-white">{r.bookings}</td><td className="p-4 text-emerald-400">฿{r.revenue}</td></tr>)}</tbody></table></div></div>}
