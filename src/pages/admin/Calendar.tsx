import {useEffect,useState} from 'react';
type B={id:number;studio_name:string;user_name:string;date:string;start_time:string;end_time:string;status:string};
export default function AdminCalendar(){const [rows,setRows]=useState<B[]>([]);useEffect(()=>{fetch('/api/admin/calendar').then(r=>r.json()).then(setRows)},[]);return <div className="space-y-6"><div><h1 className="text-2xl font-bold text-white">ปฏิทินการจอง</h1><p className="text-zinc-500 text-sm mt-1">รายการจากฐานข้อมูลจริง</p></div><div className="space-y-3">{rows.length===0?<div className="text-zinc-500">ยังไม่มีการจอง</div>:rows.map(b=><div key={b.id} className="bg-[#111114] border border-white/6 rounded-xl p-4 flex flex-wrap justify-between gap-3"><div><p className="text-white font-medium">{b.studio_name}</p><p className="text-zinc-500 text-sm">{b.user_name}</p></div><div className="text-zinc-300 text-sm">{b.date} · {b.start_time}–{b.end_time}</div><span className="text-xs text-violet-400 px-2 py-1 rounded-full bg-violet-500/10">{b.status}</span></div>)}</div></div>}
